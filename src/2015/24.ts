import aoc from "../lib";

const input = aoc.load(Number);

export function day24() {
	// function find(groups: number): number {
	// 	const weight = input.sum() / groups;
	// 	let configurations: number[][] = [];
	// 	for (let i = 1; !configurations.length; ) {
	// 		configurations = input.combinations(i++).filter((x) => Math.sum(...x) === weight);
	// 	}
	// 	return configurations.map((x) => Math.prod(...x)).sort((a, b) => a - b)[0];
	// }
	// return [3, 4].map(find);
}
