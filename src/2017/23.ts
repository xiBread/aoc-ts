import aoc from "../lib";

const [input] = aoc.load();

export function day23() {
	// const first = +input.split(' ')[2];
	// const offset = first * 100 + 1e5;
	// return [(first - 2) ** 2, -~[...Number.range(offset, offset + 17e3, 17)].filter((x) => !Math.isPrime(x)).length];
}
