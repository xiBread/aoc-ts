import { invert } from "lodash";
import aoc from "../lib";

const input = aoc.load();

export function day07() {
	return [];
	// const bottom = invert(
	// 	input
	// 		.map((line) => line.match(/[a-z]+/g)!)
	// 		.flat()
	// 		.sort()
	// 		.countBy()
	// )[1];

	// return [bottom];
}
